# 趣学伴整体 ToDo

更新时间：2026-05-01

## 当前执行原则

本文件是当前唯一的总 ToDo。旧版本任务、历史发布记录和旧路线图已经按类型归档到 `docs/todos/`、`docs/releases/` 和 `docs/roadmaps/`。

当前产品方向：

- 小学 1.9 正式采用“三层准备度模型”：交付层、认知层、稳定性层。
- 小学三公方向不再按“五大核心 + 四个支撑模型”同级展示；五大核心归入交付层，执行支撑拆入稳定性层、数据证据和诊断引擎。
- 初中版本采用同一底层引擎，但换成初中目标配置：学科交付、认知方法、稳定性系统。
- 1.8 只收尾当前已经展开的任务详情和阶段化工作。
- 1.9 的重点是三层模型、目标引擎、任务模型整理、稳定性层基础数据和轻量诊断卡片。
- 能力掌握度、复杂诊断、目标校适配度、仪表盘重构和 AI 报告都等 1.9 基础闭环结束后再做。
- 2.0 前完成主体功能闭环；2.0 的核心是全面引入 AI，让系统真正被 AI 驱动。

## P0：1.8 上线归档

### 1. 任务编辑入口统一

状态：已完成，发布前检查已记录。

已完成：

- 任务列表卡片点击进入任务详情。
- 任务列表右上角操作语义改为“进入详情”。
- 任务列表页已移除直接编辑弹窗和旧编辑 mutation。
- 抽出统一 `TaskEditor`。
- 新建任务和任务详情编辑共用同一套字段和保存逻辑。
- 保存后任务列表、任务详情、今日概览、学习计划展示一致。

验收：

- 不存在一个入口能保存、另一个入口丢字段。
- 新建和编辑均覆盖任务名称、分类、学科、完成方式、难度、分配规则、频次、预计时长、任务类型、年级、能力分类、能力点、关联目标、目标类型、时间块、记录方式、单位、目标值。

### 2. 任务详情页最终验收

状态：已完成，发布前检查已记录。

已完成：

- 确认详情页展示完整任务配置。
- 确认详情页编辑保存不丢阶段化字段。
- 确认删除、推送钉钉、返回列表等操作可用。
- 展示基础执行历史：完成记录、专注记录、进度或质量记录。

验收：

- 详情页承担查看、编辑、复盘和解释。
- 列表页不再承担直接编辑。

### 3. 能力模型命名调整

状态：移入 1.9 三层准备度模型定型。

范围：

- 小学 1.9 对外口径统一为“三层准备度模型”。
- 初中 1.9 对外口径统一为“初中目标配置”，底层仍复用三层模型抽象。
- 页面文案、帮助文案、阶段化设计文档和任务字段说明同步调整。

验收：

- 小学文案明确围绕三公准备度、目标校适配和长期稳定输出。
- 初中文案明确围绕学科交付、认知方法、复习策略和稳定性管理。

### 4. 1.8 全链路回归

状态：已上线。

回归路径：

- 新建小学任务，检查成长能力点、目标类型、时间块、今日概览展示。
- 新建初中任务，检查学科、学科能力点、目标类型、今日概览展示。
- 编辑旧任务，补齐能力点和目标类型后保存。
- 切换孩子，确认任务、阶段、今日概览和钉钉文案不串。
- 发布学习计划，确认任务字段进入计划展示。
- 钉钉推送与页面口径一致。

验收：

- `docs/releases/release-1.8.0.md` 已记录发布前本地检查。
- 1.8 已上线，后续不再继续扩 1.8 范围。
- 新功能进入 1.9 三层准备度模型与目标引擎。

## P1：1.9 三层准备度模型与目标引擎

设计入口：`docs/design-1.9-ability-goal-engine.md`。

### 5. 三层准备度模型定型

状态：1.9.0 首轮已落地，后端配置化和权重体系进入 1.9.x。

范围：

- 小学三公模板定型为交付层、认知层、稳定性层。
- 交付层包含英语、数理、大语文、项目成果、临场应变。
- 认知层包含规则内化、迁移应用、信息处理速度、逻辑深度、批判思维。
- 稳定性层包含睡眠、运动、情绪、完成率、延期率、复盘频率和外部负载。
- 初中版本按同一底层抽象预留：学段、目标、层级、模块、指标、规则、权重、建议模板。
- 家庭级编辑先轻量化，优先允许编辑目标和能力点文案；全层级模板编辑后置。

### 6. 目标引擎升级

状态：1.9.0 首轮已落地。

范围：

- 目标体系与三层准备度模型匹配。
- 目标成为独立对象，绑定能力点、周期、状态、成功标准和复盘节奏。
- 小学目标支持三公基础模板，后续扩展上实、上外附中、浦外权重。
- 初中目标支持中考、自招、特色高中、国际路线等后续目标配置。
- 任务必须能解释服务哪个目标和哪个能力点。
- 目标完成情况进入首页建议、目标页和报告摘要。

### 6.1 轻量诊断卡片

状态：1.9.0 首轮已落地。

范围：

- 首页或能力模型页增加“本周建议”区域。
- 第一版只输出冲刺、修复、减压、维持四类策略。
- 首批诊断覆盖高压透支、执行断裂、外部扰动、数据不足。
- 所有建议必须显示证据来源或“证据不足”提示。
- 复杂诊断，例如假性退步、假性爆发、认知阻力位，放入 1.9.x。

### 6.2 稳定性层基础数据

状态：1.9.0 首轮已落地，独立 schema 和统计 API 后置。

范围：

- 完成率、延期率、复盘频率进入稳定性层。
- 睡眠、运动、情绪进入稳定性层。
- 增加外部负载记录：考试周、生病、旅行、换老师、家庭节奏异常。
- 稳定性层支持高压透支和执行断裂的基础判断。

### 6.3 数理认知试点字段

状态：1.9.0 首轮已落地，当前只沉淀证据，不做自动评分。

范围：

- 数理任务支持认知标签：规则内化、迁移应用、信息处理速度、逻辑深度。
- 记录尝试次数、耗时、是否提示、错因、复盘质量。
- 1.9 只沉淀证据，不做自动认知评分。

### 6.4 任务列表分配字段重构

状态：1.9.0 首轮已落地。

范围：

- 重新梳理任务列表里的“分配规则、频次、记录方式、阶段字段、目标类型、时间块”的展示优先级。
- 区分交付层、认知层、稳定性层任务的列表摘要，不再把旧字段机械堆叠在卡片上。
- 对旧任务缺字段给出低干扰提示，避免列表页显示不合理标签。

### 7. 学制配置

状态：未开始，可并入 1.9 或 1.9.x。

范围：

- 孩子档案增加 `schoolSystem`：五四制、六三制。
- 阶段判断支持 `schoolSystem + grade -> educationStage`。
- 五四制：1-5 年级小学，6-9 年级初中。
- 六三制：1-6 年级小学，7-9 年级初中。
- 小学三层准备度模型可按低段、中段、高段表达，高段对应小学最后 1-2 年。

不做：

- 地区教材版本。
- 完整课程进度。
- 精细知识点体系。

## P2：1.9.x 治理、规则能力和仪表盘

### 8. 数据质量轻量版

状态：待能力模型定型后判断。

解释：

数据质量轻量版是“数据体检入口”，用于发现 AI、能力掌握度和报告之前的数据缺口。它不是复杂中心，第一版只做检查和跳转修复。

候选检查范围：

- 任务缺能力点。
- 任务缺目标类型。
- 目标缺能力点。
- 图书缺 ISBN、页数、类型。
- 阅读记录缺日期、页数、时长。
- 打卡记录缺质量评价或家长观察。

### 9. 规则版诊断引擎

状态：待能力模型和目标引擎结束后做。

范围：

- 增加置信度：样本数量、数据来源数量、最近更新时间。
- 增加 Velocity：4 周/8 周成长斜率、稳定性波动率。
- 增加假性退步、假性爆发、认知阻力位、输入不足、输出虚高。
- 增加交叉验证：同一认知指标在英语、数理、大语文之间横向对比。
- 增加余力指数：强项维持、短板加码、过载减压。
- 展示规则说明，避免系统给出黑盒结论。

### 10. 仪表盘重构

状态：后置，2.0 以前完成。

范围：

- 围绕三层准备度模型重构仪表盘。
- 展示交付层 Gap、认知层证据、稳定性风险、目标进度和下一步建议。
- 支持小学三公和初中版本的不同目标配置。
- 先规则化展示，不接 AI。

### 10.1 目标校适配度

状态：后置，1.9 基础闭环完成后做。

范围：

- 三公基础模板扩展为上实、上外附中、浦外权重配置。
- 展示不同目标校适配度，不只展示一个准备度总分。
- 初中版本扩展中考、自招、特色高中、国际路线权重配置。

### 11. 阅读成长档案

状态：后置。

范围：

- 每本书展示阅读次数、总时长、总页数、最近阅读、阶段变化。
- 阅读记录形成时间线。
- 阅读能力标签逐步接入能力模型。

## P3：2.0 AI 驱动

### 12. 学习事件日志

状态：2.0 前置。

范围：

- 任务创建、任务编辑、计划发布、任务完成、任务延期、阅读记录、表现记录等事件入库。
- 事件可被报告和 AI 读取。

### 13. AI 驱动能力

状态：2.0 核心。

范围：

- AI 学习画像。
- AI 发现数据缺口。
- AI 解释能力变化。
- AI 目标建议。
- AI 任务推荐草稿。
- AI 阅读建议。
- AI 周报/月报草稿。
- AI 家长问答。
- AI 根据执行情况调整建议。

约束：

- AI 不自动执行关键操作。
- AI 输出必须引用真实数据。
- 数据不足时必须说明。
- 家长可关闭、纠偏和否定建议。

## 明确不做或后置

- 1.8 不做能力掌握度。
- 1.8 不做数据质量中心。
- 1.8 不做目标引擎。
- 1.8 不做仪表盘重构。
- 1.8 不做完整知识图谱。
- 1.8 不做目标学校数据库。
- 1.8 不做名校/三公/重点中学差距量化看板。
- 1.8 不做小学/初中双页面大改版。
- 1.8 不做 AI 学习路径规划。
- 1.8 不做 Prisma 7、Express 5、Vite 8 等大版本升级。
- 服务器多应用目录迁移单独安排维护窗口，不混入业务发版。
