# 趣学伴 1.9.x 数据质量轻量版检查记录

日期：2026-05-02

## 范围

本轮完成 P2 的数据质量轻量版首版入口，不新增数据库 schema，不做自动修复中心，只提供跨模块缺口预检和跳转修复。

## 已落地

- 新增家长端页面 `/parent/data-quality`。
- 侧边栏“复盘分析”增加“数据体检”入口。
- 数据体检按当前选择孩子检查：
  - 任务缺能力点。
  - 任务缺目标类型。
  - 目标缺能力点或能力分类。
  - 图书缺 ISBN、页数或类型。
  - 在读记录缺日期、当前页数或图书总页数。
  - 指定日期打卡缺质量评价或家长观察。
- 页面提供：
  - 检查分组、完整分组、待补缺口、检查样本汇总。
  - 每类缺口数量、比例、示例和跳转修复入口。
  - 日期切换，用于检查不同日期的打卡质量。
  - 修复顺序建议。

## 规则诊断 V0.1

增补日期：2026-05-03

- 在数据体检页增加“规则诊断 V0.1”区域。
- 读取近 8 周打卡记录，比较最近 4 周与前 4 周完成率，并保留近 14 天变化。
- 每条诊断展示：
  - 置信度。
  - 样本数量。
  - 数据来源。
  - 更新时间。
  - 证据。
  - 规则说明。
- 首批规则：
  - 数据充足度。
  - 执行断裂。
  - 高压透支。
  - 余力指数。
  - 跨学科交叉验证。
  - 输入不足。
  - 输出虚高。

## 本地检查

已执行：

```bash
pnpm --dir frontend lint
pnpm --dir frontend build
```

结果：

- `frontend lint` 通过。
- `frontend build` 通过。

## 后置

- 不做批量自动修复。
- 不新增独立数据质量后端 API。
- 跨学科交叉验证当前按任务名称、分类、学科和能力点做轻量归类，后续可迁移到独立规则配置。
- 不接 AI 报告，只为后续 AI 和诊断准备数据入口。

## 仪表盘接入

增补日期：2026-05-03

- 成长仪表盘的“成长总览”顶部接入三层准备度诊断摘要。
- 成长仪表盘的“执行力分析”替换静态 mock：
  - 综合评分使用当前范围完成率、负向控制、延期控制、稳定性和余力指数加权。
  - 趋势图使用真实完成率趋势。
  - 关注任务使用当前范围内未完成和延期较多的任务。
  - 改进建议使用规则诊断摘要和负向状态生成。
- 成长仪表盘的“学科能力”替换静态 mock：
  - 按任务名称、分类、学科和能力点归类语文、数学、英语、科学、综合素养。
  - 按近 8 周完成率、质量证据、认知记录和负向状态计算学科分。
  - 展示弱项证据拆解和横向结论。
- 成长仪表盘的“心态兴趣”替换静态 mock：
  - 按任务分类生成兴趣偏好。
  - 按情绪、睡眠、外部负载、专注分钟和余力指数计算兴趣、主动性、专注度和学习动力。
  - 展示稳定性拆解、心态证据和兴趣发展建议。
- 摘要展示：
  - 当前状态。
  - 余力指数。
  - 近 8 周证据。
  - 下一步动作。
- 保留“查看完整诊断”跳转到 `/parent/data-quality`。
